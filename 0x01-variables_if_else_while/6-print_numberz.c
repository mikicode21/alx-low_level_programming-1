#include <stdio.h>

/**
 * main - print all single digits number of base 10 starting from 0
 * Return: 0 always
 */
int main(void)
{
	int i;

	for (i = 0; i < 0; i++)
	{
		putchar(i % 10) + 0;
	}
	putchar('\n');
	return (0);
}
